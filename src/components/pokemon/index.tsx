import React from 'react';

type Props = { id: number, name: string, height: number, weight: number };
function Pokemon(props: Props) {
  return(
    <section>
      <h1>{name}</h1>
      
    </section>
  )
}